<?xml version="1.0"?>
<launch>
  <!-- 
    Launch FAST-LIO2 mapping only (assumes driver is already running or using rosbag)
    Useful for testing with recorded data
  -->

  <arg name="rviz" default="true" doc="Launch RVIZ visualization"/>

  <!-- Load FAST-LIO2 parameters -->
  <rosparam command="load" file="$(find fast_lio)/config/mid360.yaml" />

  <!-- FAST-LIO2 Parameters -->
  <param name="feature_extract_enable" type="bool" value="0"/>
  <param name="point_filter_num" type="int" value="3"/>
  <param name="max_iteration" type="int" value="3"/>
  <param name="filter_size_surf" type="double" value="0.5"/>
  <param name="filter_size_map" type="double" value="0.5"/>
  <param name="cube_side_length" type="double" value="200"/>
  <param name="runtime_pos_log_enable" type="bool" value="0"/>

  <!-- Launch FAST-LIO2 mapping node -->
  <node pkg="fast_lio" type="fastlio_mapping" name="laserMapping" output="screen" />

  <!-- RVIZ Visualization -->
  <group if="$(arg rviz)">
    <node launch-prefix="nice" pkg="rviz" type="rviz" name="rviz" 
          args="-d $(find fast_lio)/rviz_cfg/loam_livox.rviz" />
  </group>

</launch>
